prereqs:
  runtime:
    requires:
      App::FatPacker:
      Distribution::Metadata: { version: '0.05' }
      Getopt::Long: { version: '2.39' }
      Perl::Strip:
      perl: { version: 'v5.16' }
  develop:
    requires:
      File::pushd:
      Capture::Tiny:
